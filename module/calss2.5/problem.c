#include <stdio.h>

int main()
{
    int a, b, c, result;

    printf("Enter the a :  \n");
    scanf("%d", &a);
    printf("Enter the b  \n", b);
    scanf("%d", b);

    result = a * b;
    printf("The Area is  %d\n", result);
    int sum = a + b;
    float total = 2 * sum;

    printf("The total Area is %f\n", total);
}